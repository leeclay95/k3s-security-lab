apiVersion: v1
kind: Pod
metadata:
  name: insecure-demo
  namespace: {{ .Release.Namespace }}
spec:
  containers:
    - name: bad
      image: busybox:1.36
      securityContext:
        privileged: true
