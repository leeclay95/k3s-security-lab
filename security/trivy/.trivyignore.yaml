# Trivy risk-acceptance register  (Gate 4 — scripts/security-gates.sh)
#
# These are KNOWN, fixable HIGH/CRITICAL CVEs in the lab's pinned base image
# (docker.io/nginxinc/nginx-unprivileged:1.27, Debian bookworm). They live in
# the OS/library layer, not app code, and are accepted for this LAB so the gate
# stays green on the intentionally-pinned image. Each entry auto-expires on
# 2027-01-11 — after that the gate re-flags them, forcing a re-review or image bump.
#
# This is a scoped exception list, NOT a blanket disable: any NEW CVE (or one not
# listed here) still fails Gate 4. The real remediation is to bump the image to a
# patched tag (e.g. stable-alpine); see security/COMPLIANCE.md (Gate 4).
#   generated 2026-07-15 from a trivy --ignore-unfixed --severity HIGH,CRITICAL scan

vulnerabilities:
  - id: CVE-2026-33845
    # CRITICAL  libgnutls30 3.7.9-2+deb12u4 -> fixed 3.7.9-2+deb12u7
    statement: >-
      Base-OS CVE in libgnutls30 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-42010
    # CRITICAL  libgnutls30 3.7.9-2+deb12u4 -> fixed 3.7.9-2+deb12u7
    statement: >-
      Base-OS CVE in libgnutls30 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-31789
    # CRITICAL  libssl3 3.0.16-1~deb12u1 -> fixed 3.0.19-1~deb12u2
    statement: >-
      Base-OS CVE in libssl3 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2024-56171
    # CRITICAL  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u2
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-5773
    # HIGH  curl 7.88.1-10+deb12u12 -> fixed 7.88.1-10+deb12u15
    statement: >-
      Base-OS CVE in curl from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-68973
    # HIGH  gpgv 2.2.40-1.1 -> fixed 2.2.40-1.1+deb12u2
    statement: >-
      Base-OS CVE in gpgv from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-4878
    # HIGH  libcap2 1:2.66-4+deb12u1 -> fixed 1:2.66-4+deb12u3
    statement: >-
      Base-OS CVE in libcap2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2023-52425
    # HIGH  libexpat1 2.5.0-1+deb12u1 -> fixed 2.5.0-1+deb12u2
    statement: >-
      Base-OS CVE in libexpat1 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-32988
    # HIGH  libgnutls30 3.7.9-2+deb12u4 -> fixed 3.7.9-2+deb12u5
    statement: >-
      Base-OS CVE in libgnutls30 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-32990
    # HIGH  libgnutls30 3.7.9-2+deb12u4 -> fixed 3.7.9-2+deb12u5
    statement: >-
      Base-OS CVE in libgnutls30 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-33846
    # HIGH  libgnutls30 3.7.9-2+deb12u4 -> fixed 3.7.9-2+deb12u7
    statement: >-
      Base-OS CVE in libgnutls30 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-3833
    # HIGH  libgnutls30 3.7.9-2+deb12u4 -> fixed 3.7.9-2+deb12u7
    statement: >-
      Base-OS CVE in libgnutls30 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-42009
    # HIGH  libgnutls30 3.7.9-2+deb12u4 -> fixed 3.7.9-2+deb12u7
    statement: >-
      Base-OS CVE in libgnutls30 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-40355
    # HIGH  libgssapi-krb5-2 1.20.1-2+deb12u3 -> fixed 1.20.1-2+deb12u5
    statement: >-
      Base-OS CVE in libgssapi-krb5-2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-40356
    # HIGH  libgssapi-krb5-2 1.20.1-2+deb12u3 -> fixed 1.20.1-2+deb12u5
    statement: >-
      Base-OS CVE in libgssapi-krb5-2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-27135
    # HIGH  libnghttp2-14 1.52.0-1+deb12u2 -> fixed 1.52.0-1+deb12u3
    statement: >-
      Base-OS CVE in libnghttp2-14 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-6020
    # HIGH  libpam-modules 1.5.2-6+deb12u1 -> fixed 1.5.2-6+deb12u2
    statement: >-
      Base-OS CVE in libpam-modules from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-64720
    # HIGH  libpng16-16 1.6.39-2 -> fixed 1.6.39-2+deb12u1
    statement: >-
      Base-OS CVE in libpng16-16 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-65018
    # HIGH  libpng16-16 1.6.39-2 -> fixed 1.6.39-2+deb12u1
    statement: >-
      Base-OS CVE in libpng16-16 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-66293
    # HIGH  libpng16-16 1.6.39-2 -> fixed 1.6.39-2+deb12u1
    statement: >-
      Base-OS CVE in libpng16-16 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-22695
    # HIGH  libpng16-16 1.6.39-2 -> fixed 1.6.39-2+deb12u2
    statement: >-
      Base-OS CVE in libpng16-16 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-22801
    # HIGH  libpng16-16 1.6.39-2 -> fixed 1.6.39-2+deb12u2
    statement: >-
      Base-OS CVE in libpng16-16 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-25646
    # HIGH  libpng16-16 1.6.39-2 -> fixed 1.6.39-2+deb12u3
    statement: >-
      Base-OS CVE in libpng16-16 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-15467
    # HIGH  libssl3 3.0.16-1~deb12u1 -> fixed 3.0.18-1~deb12u2
    statement: >-
      Base-OS CVE in libssl3 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-69421
    # HIGH  libssl3 3.0.16-1~deb12u1 -> fixed 3.0.18-1~deb12u2
    statement: >-
      Base-OS CVE in libssl3 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-28387
    # HIGH  libssl3 3.0.16-1~deb12u1 -> fixed 3.0.19-1~deb12u2
    statement: >-
      Base-OS CVE in libssl3 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-28388
    # HIGH  libssl3 3.0.16-1~deb12u1 -> fixed 3.0.19-1~deb12u2
    statement: >-
      Base-OS CVE in libssl3 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-28389
    # HIGH  libssl3 3.0.16-1~deb12u1 -> fixed 3.0.19-1~deb12u2
    statement: >-
      Base-OS CVE in libssl3 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-28390
    # HIGH  libssl3 3.0.16-1~deb12u1 -> fixed 3.0.19-1~deb12u2
    statement: >-
      Base-OS CVE in libssl3 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-45447
    # HIGH  libssl3 3.0.16-1~deb12u1 -> fixed 3.0.20-1~deb12u2
    statement: >-
      Base-OS CVE in libssl3 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-9900
    # HIGH  libtiff6 4.5.0-6+deb12u2 -> fixed 4.5.0-6+deb12u3
    statement: >-
      Base-OS CVE in libtiff6 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2026-4775
    # HIGH  libtiff6 4.5.0-6+deb12u2 -> fixed 4.5.0-6+deb12u4
    statement: >-
      Base-OS CVE in libtiff6 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2022-49043
    # HIGH  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u2
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2024-25062
    # HIGH  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u2
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-24928
    # HIGH  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u2
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-27113
    # HIGH  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u2
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-32414
    # HIGH  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u2
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-32415
    # HIGH  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u2
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-49794
    # HIGH  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u3
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-49796
    # HIGH  libxml2 2.9.14+dfsg-1.3~deb12u1 -> fixed 2.9.14+dfsg-1.3~deb12u3
    statement: >-
      Base-OS CVE in libxml2 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2025-7424
    # HIGH  libxslt1.1 1.1.35-1+deb12u1 -> fixed 1.1.35-1+deb12u2
    statement: >-
      Base-OS CVE in libxslt1.1 from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
  - id: CVE-2023-31484
    # HIGH  perl-base 5.36.0-7+deb12u2 -> fixed 5.36.0-7+deb12u3
    statement: >-
      Base-OS CVE in perl-base from the pinned lab image; not reachable via the
      nginx workload's exposed surface. Accepted for the lab; remediate by
      bumping the image tag.
    expired_at: 2027-01-11
